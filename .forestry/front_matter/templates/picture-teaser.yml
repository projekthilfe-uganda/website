---
label: teaser block
hide_body: false
fields:
- type: text
  name: type
  label: Type
  hidden: true
  default: teaserblock
- type: text
  name: title
  label: Name
  description: Internal, not visible name.
- type: number
  name: order
  label: order
  description: Sort order on page
  config:
    step: '1'
- type: field_group_list
  name: teaser
  label: Teaser
  description: Add teaser elements here.
  config:
    min: '2'
    labelField: title
  fields:
  - type: text
    name: title
    label: Title
    config:
      required: true
  - type: textarea
    name: text
    label: Text
    config:
      wysiwyg: true
      schema:
        format: markdown
  - type: file
    name: picture
    label: Picture
pages:
- content/home/menu-teaser.md
