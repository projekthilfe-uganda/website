---
label: shop project
hide_body: false
fields:
- name: type
  type: text
  config:
    required: true
  label: Type
  hidden: true
  default: shopproject
- name: title
  type: text
  config:
    required: false
  label: Title
- name: identifier
  type: text
  config:
    required: true
  label: Identifier
  description: Identifier for item that should never be changed once in use.
- name: picture
  type: file
  config:
    maxSize: 64
  label: Picture
- name: order
  type: number
  label: Order
  description: ''
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
